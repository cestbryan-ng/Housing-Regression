# Forêt aléatoire

Ce dépôt contient une implémentation pratique du modèle de random forest et une comparaison avec le modèle linéaire simple. L'objectif est créer plusieurs arbres de décisions et d'en déduire le résultat par un vote majoritaire(clustering) ou une moyenne de prédiction de tous les arbres.


## Set-Up
```bash
git clone https://github.com/cestbryan-ng/Random-Forest.git
cd Random-Forest

pip install -r requirements.txt
```